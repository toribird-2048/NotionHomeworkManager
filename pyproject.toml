[project]
name = "notionhomeworkmanager-1"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "fly-toribird", email = "fly-toribird@outlook.jp" }
]
dependencies = [
    "notion-client>=2.7.0",
    "python-dotenv>=1.2.1",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/notionhomeworkmanager_1"]
